mutation addMovie($input: MovieInput!) {
    addMovie(input: $input) {
        id
        createdAt
        updatedAt
        releasedAt
        title
        description
        reason
        poster
        rating
        creatorId
    }
}
